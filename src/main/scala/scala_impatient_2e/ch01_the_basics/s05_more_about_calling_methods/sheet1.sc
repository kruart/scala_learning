"Hello".intersect("World")
"Hello" intersect "World"

"Bonjour".sorted

// In Scala, the _ character is a "wildcard," like * in Java
import scala.math._

sqrt(2)
pow(2, 4)
min(3, Pi)

BigInt.probablePrime(100, scala.util.Random)